import React from "react";
import ReactDOM from "react-dom/client";
import StellarAssault from "./StellarAssault";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StellarAssault />
  </React.StrictMode>,
);
